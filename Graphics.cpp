// #include "Graphics.h"

// void Graphics::drawMap(SDL_Renderer *renderer, const Map &map)
// {
//    for (int y = 0; y < map.getHeight(); ++y) {
//       for (int x = 0; x < map.getWidth(); ++x) {
//          if (map.getTile(x, y) == Tile::WALL) {
//             SDL_Rect rect = {x * 32, y * 32, 32,
//          }
//       }
//    }
// }
